import { Component } from '@angular/core';

// todo: split into components -> tasks, task, controls
// todo: add localStorage persistence of tasks
// todo: add filters into the ngFor

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
}
